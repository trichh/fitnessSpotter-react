// Importing react
import React from "react";
// Exporting the Login page to use in ../main.js as the page on the "/login" route
export default class Login extends React.Component {
  render() {
    return (
      <div>
        <h1>Login Page</h1>
      </div>
    );
  }
}
